# CI：文档门禁 + NestJS API（Prisma / Postgres）
# 详见 docs/13-ci-cd-spec.md

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify documentation layout
        run: |
          set -euo pipefail
          test -f docs/README.md
          test -f docs/00-development-standards.md
          test -f docs/13-ci-cd-spec.md

  backend:
    needs: docs
    runs-on: ubuntu-latest
    permissions:
      contents: read

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: app
          POSTGRES_PASSWORD: app
          POSTGRES_DB: exam_sys
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U app -d exam_sys"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://app:app@localhost:5432/exam_sys?schema=public
      JWT_ACCESS_SECRET: ci_access_secret_change_me
      JWT_REFRESH_SECRET: ci_refresh_secret_change_me

    defaults:
      run:
        working-directory: apps/api

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect backend workspace
        id: detect_backend
        run: |
          if [ -f "apps/api/package-lock.json" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "apps/api/package-lock.json not found; backend job steps will be skipped."
          fi

      - name: Setup Node.js
        if: steps.detect_backend.outputs.enabled == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/api/package-lock.json

      - name: Install dependencies
        if: steps.detect_backend.outputs.enabled == 'true'
        run: npm ci

      - name: Prisma migrate (deploy)
        if: steps.detect_backend.outputs.enabled == 'true'
        run: npx prisma migrate deploy

      - name: Lint
        if: steps.detect_backend.outputs.enabled == 'true'
        run: npm run lint

      - name: Build
        if: steps.detect_backend.outputs.enabled == 'true'
        run: npm run build

      - name: Unit tests
        if: steps.detect_backend.outputs.enabled == 'true'
        run: npm test

      - name: E2E tests
        if: steps.detect_backend.outputs.enabled == 'true'
        run: npm run test:e2e
